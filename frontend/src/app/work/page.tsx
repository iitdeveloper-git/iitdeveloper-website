import type { Metadata } from 'next';
import Link from 'next/link';
import { ArrowRight } from 'lucide-react';
import PageShell from '@/components/layout/PageShell';
import { caseStudies } from '@/content/work';
import { generateSEO } from '@/lib/seo';
export const metadata: Metadata = generateSEO({ title: 'Selected Products and Client Work', description: 'Explore verified IITDEVELOPER product lab initiatives and client website and software delivery.', canonical: '/work' });
export default function WorkPage() { return <PageShell><section className="pb-16 pt-36"><div className="container mx-auto px-4"><p className="text-sm font-bold tracking-[0.18em] text-secondary">WORK</p><h1 className="mt-4 text-5xl font-bold tracking-tighter sm:text-7xl">Selected Products and Client Work</h1><p className="mt-6 max-w-3xl text-xl leading-8 text-muted-foreground">Evidence-based project summaries. Where numeric outcomes are not verified, we describe the delivered solution without inventing results.</p></div></section><section className="pb-24"><div className="container mx-auto grid gap-6 px-4 md:grid-cols-2 sm:px-6 lg:px-8">{caseStudies.map((project)=><article key={project.slug} className="glass rounded-2xl border border-white/10 p-8"><p className="text-xs font-bold uppercase tracking-wider text-secondary">{project.label} · {project.status}</p><h2 className="mt-3 text-3xl font-bold">{project.title}</h2><p className="mt-4 leading-7 text-muted-foreground">{project.summary}</p><div className="mt-6 flex flex-wrap gap-2">{project.stack.map((tech)=><span key={tech} className="rounded-full bg-primary/10 px-3 py-1 text-xs">{tech}</span>)}</div><Link href={`/work/${project.slug}`} className="mt-7 inline-flex items-center font-semibold text-secondary">View case study <ArrowRight className="ml-2 h-4 w-4" /></Link></article>)}</div></section></PageShell> }
